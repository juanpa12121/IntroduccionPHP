<?php include 'includes/header.php';

/*Boolean*/
$logueado = true;
var_dump($logueado); //Para mas info

// Enteros
$numero = 200;
var_dump($numero);

// Floats EN JS SOLO NUMEROS Y YA.
$float = 200.5;
var_dump($float);

// Strings (Se puede usar " ó ')
$nombre = "Juan";
var_dump($nombre);

// Arrays
$array = [];
var_dump($array);


include 'includes/footer.php';